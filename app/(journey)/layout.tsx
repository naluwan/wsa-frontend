/**
 * Journey Layout - 課程學習頁佈局
 * 與 DashboardLayout 類似，但使用 CourseUnitSidebar
 */
export default function JourneyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>
}
